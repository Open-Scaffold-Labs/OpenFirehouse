# Frontend and serverless API both deploy automatically via Vercel on push to main.
#
# No GitHub Actions workflow is needed for deployment.
# This file is kept as a placeholder to prevent accidentally
# re-adding the old Netlify workflow.
#
# Vercel project: connected to Open-Scaffold-Labs/OpenFirehouse (frontend + serverless functions)
